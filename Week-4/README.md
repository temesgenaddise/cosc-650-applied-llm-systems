#Week-4/Multi_ Tool Assistant

The notebook defines three schema tools:

1. Lookup_weather
2. Convert_temperature
3. run_safe_math

Each schema limits incorrect or unexpected tool calls.

For each schema, I created a validation, request, and response loop.

Finally, the function call and schemas were evaluated by three queries, at least one for each, and the result was recorded.

